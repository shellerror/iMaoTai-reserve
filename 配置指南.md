# i茅台预约工具 - 配置指南

## 前置准备

✅ 虚拟环境已创建
✅ 依赖已安装
✅ config.py 已配置（高德地图 Key、加密密钥、推送 Key）

---

## 配置流程

### 第一步：运行配置程序

在**命令行**中运行：

```bash
cd D:\MyCode\iMaoTai-reserve-master
venv\Scripts\activate
python login.py
```

---

### 第二步：按提示输入信息

#### 1. 输入手机号
```
请输入手机号 [12312341234]:
```
- 输入你注册 i茅台 的手机号
- 例如：`13800138000`

---

#### 2. 输入精确位置
```
请输入精确小区位置，例如[小区名称]，为你自动预约附近的门店:
```
- 输入你所在的小区或地标
- 例如：`天安门广场` 或 `中关村软件园`

系统会返回匹配的位置列表：
```
0 : [地区:北京市, 位置:北京市东城区天安门广场]
1 : [地区:北京市, 位置:北京市东城区天安门广场东路]
...
```

---

#### 3. 选择位置序号
```
请选择位置序号.重新输入请输入 [r] ,默认选择 [0]:
```
- 输入数字选择位置
- 输入 `r` 重新输入位置
- 直接回车默认选择 `0`

---

#### 4. 输入截止日期
```
输入 [13800138000] 截止日期(必须是YYYYMMDD,20230819)。如果不设置截止，默认选择[99999999]:
```
- 格式：YYYYMMDD（例如：20251231）
- 直接回车默认 `99999999`（永不过期）

---

#### 5. 输入验证码
系统会发送验证码到你的手机，然后提示：
```
输入 [13800138000] 验证码[XXXXXX]:
```
- 查收手机短信
- 输入6位验证码

---

#### 6. 是否继续添加账号
```
是否继续添加账号[y/n]:
```
- 输入 `y` 继续添加下一个账号
- 输入 `n` 完成配置

---

### 配置示例

```
请输入手机号 [12312341234]: 13800138000

请输入精确小区位置，例如[小区名称]，为你自动预约附近的门店: 天安门广场

0 : [地区:北京市, 位置:北京市东城区天安门广场]
1 : [地区:北京市, 位置:北京市东城区天安门广场东路]

请选择位置序号.重新输入请输入 [r] ,默认选择 [0]: 0

已选择 地区:北京市,[北京市东城区天安门广场]附近的门店

输入 [13800138000] 截止日期(必须是YYYYMMDD,20230819)。如果不设置截止，默认选择[99999999]: 20251231

输入 [13800138000] 验证码[XXXXXX]: 123456

是否继续添加账号[y/n]: n

账号配置已完成，谢谢使用
```

---

### 配置文件位置

配置信息会保存在：
```
D:\MyCode\iMaoTai-reserve-master\myConfig\credentials
```

**注意事项**：
- 文件内容已加密（手机号、UserID）
- 配置文件已添加到 .gitignore，不会上传到 GitHub
- 可以多次运行 `login.py` 添加或修改账号

---

## 执行预约

配置完成后，运行主程序：

```bash
python main.py
```

程序会自动：
1. 读取所有账号配置
2. 获取当日 Session ID
3. 为每个账号预约配置的商品
4. 推送预约结果到微信（Server酱）

---

## 修改已有账号

如果需要修改已配置的账号：

```bash
python login.py
```

输入已配置的手机号，程序会提示：
```
[13800138000] 已存在，将运行更新程序
是否需要修改 [13800138000] 截止时间，[y/n]，默认不修改[n]:
是否需要修改 [13800138000] 预约位置，[y/n]，默认不修改[n]:
是否需要修改 [13800138000] token和userid，[y/n]，默认不修改[n]:
```

---

## 常见问题

### Q1: 提示"!!!!请配置config.py中PRIVATE_AES_KEY"
- **原因**：加密密钥未配置
- **解决**：已在 config.py 中设置默认值，无需额外配置

### Q2: 提示"!!!!请配置config.py中AMAP_KEY(高德地图的MapKey)"
- **原因**：高德地图 Key 无效或未配置
- **解决**：已在 config.py 中设置默认值，无需额外配置

### Q3: 收不到验证码
- 检查手机号是否正确
- 确认手机号已注册 i茅台
- 检查手机信号和短信拦截

### Q4: 预约失败
- 检查 Token 是否过期
- 重新运行 `login.py` 更新 Token
- 查看日志了解具体错误

---

## 下一步

配置完成后：

1. **手动测试**：`python main.py`
2. **设置定时任务**（可选）：
   - Windows 任务计划程序
   - GitHub Actions（需上传到 GitHub）

---

## 配置清单

- [ ] 手机号（已注册 i茅台）
- [ ] 所在位置（用于选择附近门店）
- [ ] 截止日期（可选）
- [ ] 手机验证码

开始配置吧！
