# 🚀 i茅台自动预约 - 定时任务快速启动

## ✅ 已准备的文件

| 文件名 | 说明 | 位置 |
|--------|------|------|
| `run_reservation.bat` | 实际运行的预约脚本 | ✅ 已创建 |
| `setup_scheduled_task.ps1` | PowerShell 自动安装脚本 | ✅ 已创建 |
| `一键安装定时任务.bat` | 批处理一键安装脚本 | ✅ 已创建 |

---

## 🎯 推荐安装方式（3选1）

### 方式 1：一键安装（最简单）⭐

1. **找到文件**：
   ```
   D:\MyCode\iMaoTai-reserve-master\一键安装定时任务.bat
   ```

2. **右键点击** → 选择 **"以管理员身份运行"**

3. **等待完成** → 看到"[成功]"提示

---

### 方式 2：PowerShell 安装

1. **打开 PowerShell（管理员）**
   - 按 `Win + X`
   - 选择 "Windows PowerShell (管理员)"

2. **运行命令**：
   ```powershell
   cd D:\MyCode\iMaoTai-reserve-master
   .\setup_scheduled_task.ps1
   ```

3. **按提示操作** → 脚本会自动完成配置

---

### 方式 3：手动创建

1. 按 `Win + R`，输入 `taskschd.msc`

2. 点击右侧 "创建基本任务"

3. 按以下信息填写：
   - 名称：`i茅台自动预约`
   - 触发器：每天，09:05:00
   - 操作：启动程序
   - 程序：`D:\MyCode\iMaoTai-reserve-master\run_reservation.bat`
   - 起始于：`D:\MyCode\iMaoTai-reserve-master`

4. 完成后勾选 "打开任务属性对话框"

5. 在属性中：
   - ✅ 勾选 "不管用户是否登录都要运行"
   - ✅ 勾选 "使用最高权限运行"

6. 点击 "确定"

---

## ✅ 验证是否成功

运行以下命令查看任务：

**PowerShell**：
```powershell
Get-ScheduledTask -TaskName "iMaoTi*" | Format-List
```

**命令提示符**：
```cmd
schtasks /query /tn "iMaoTai_Reserve"
```

**或直接打开**：
```
Win + R → taskschd.msc → 查找 "i茅台" 任务
```

---

## 🧪 立即测试

**方法 1：任务计划程序**
1. 打开任务计划程序
2. 找到 "i茅台" 任务
3. 右键 → "运行"

**方法 2：命令行**
```cmd
schtasks /run /tn "iMaoTai_Reserve"
```

**方法 3：直接运行脚本**
```cmd
D:\MyCode\iMaoTai-reserve-master\run_reservation.bat
```

⚠️ **注意**：当前时间（00:03）不在预约时段（9:00-12:00），测试会返回"申购暂未开始"，这是正常的。

---

## 📋 任务配置详情

| 项目 | 值 |
|------|-----|
| 任务名称 | iMaoTai_AutoReserve |
| 运行时间 | 每天 09:05 |
| 运行用户 | SYSTEM |
| 运行权限 | 最高权限 |
| 运行脚本 | run_reservation.bat |

---

## 📝 常用命令

| 操作 | 命令 |
|------|------|
| 查看任务 | `Get-ScheduledTask -TaskName "iMaoTi*"` |
| 立即运行 | `schtasks /run /tn "iMaoTi_Reserve"` |
| 禁用任务 | `Disable-ScheduledTask -TaskName "iMaoTi*"` |
| 启用任务 | `Enable-ScheduledTask -TaskName "iMaoTi*"` |
| 删除任务 | `schtasks /delete /tn "iMaoTi_Reserve" /f` |

---

## 🎉 完成后

✅ 每天早上 09:05 自动运行预约程序
✅ 预约成功会推送到微信（Server酱）
✅ 无需任何手动操作

**明天早上 09:05，程序会自动运行！**

---

## ❓ 常见问题

**Q: 如何修改运行时间？**
A: 打开任务计划程序 → 右键任务 → 属性 → 触发器 → 编辑

**Q: 如何查看运行日志？**
A: 任务计划程序 → 任务 → 历史记录（需启用）

**Q: 任务没有运行怎么办？**
A:
1. 检查系统时间
2. 确认任务已启用
3. 手动运行测试

---

**祝预约成功！** 🍶
