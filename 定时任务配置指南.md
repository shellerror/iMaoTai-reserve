# i茅台预约工具 - 定时任务配置指南

## 方式一：自动安装（推荐）

### 步骤 1：以管理员身份运行 PowerShell

1. 按 `Win + X` 键
2. 选择 "Windows PowerShell (管理员)" 或 "终端 (管理员)"

### 步骤 2：运行安装脚本

```powershell
cd D:\MyCode\iMaoTai-reserve-master
.\setup_scheduled_task.ps1
```

### 步骤 3：确认安装

- 脚本会自动创建定时任务
- 任务名称：`i茅台自动预约`
- 运行时间：每天 09:05

---

## 方式二：手动配置

### 步骤 1：打开任务计划程序

1. 按 `Win + R` 键
2. 输入 `taskschd.msc`
3. 按回车

### 步骤 2：创建基本任务

1. 右侧点击 "创建基本任务"
2. 名称：`i茅台自动预约`
3. 描述：`每天9:05自动预约茅台`
4. 点击 "下一步"

### 步骤 3：设置触发器

1. 选择 "每天"
2. 点击 "下一步"
3. 开始时间：`09:05:00`
4. 重复周期：每天
5. 点击 "下一步"

### 步骤 4：设置操作

1. 选择 "启动程序"
2. 点击 "下一步"
3. 程序/脚本：`D:\MyCode\iMaoTai-reserve-master\run_reservation.bat`
4. 起始于：`D:\MyCode\iMaoTai-reserve-master`
5. 点击 "下一步"

### 步骤 5：完成

1. 勾选 "当单击完成时，打开此任务属性的对话框"
2. 点击 "完成"

### 步骤 6：高级设置（可选）

在属性对话框中：

1. 切换到 "常规" 标签
   - ✅ 勾选 "不管用户是否登录都要运行"
   - ✅ 勾选 "使用最高权限运行"

2. 切换到 "条件" 标签
   - ❌ 取消勾选 "只有在计算机使用交流电源时才启动此任务"
   - ❌ 取消勾选 "如果计算机使用电池模式则停止"

3. 切换到 "设置" 标签
   - ✅ 勾选 "如果任务失败，按以下频率重新启动"
   - 间隔：5 分钟
   - 尝试次数：3 次
   - ✅ 勾选 "如果任务运行时间超过以下时间，停止任务"
   - 时间：1 小时

4. 点击 "确定"

---

## 验证任务

### 查看任务列表

在任务计划程序中：
1. 展开 "任务计划程序库"
2. 找到 "i茅台自动预约" 任务
3. 查看下次运行时间

### 立即测试运行

**方法 1：通过任务计划程序**
1. 右键点击 "i茅台自动预约" 任务
2. 选择 "运行"

**方法 2：通过 PowerShell**
```powershell
Start-ScheduledTask -TaskName "i茅台自动预约"
```

**方法 3：通过命令提示符**
```cmd
schtasks /run /tn "i茅台自动预约"
```

---

## 管理任务

### 查看任务状态
```powershell
Get-ScheduledTask -TaskName "i茅台自动预约"
```

### 禁用任务
```powershell
Disable-ScheduledTask -TaskName "i茅台自动预约"
```

### 启用任务
```powershell
Enable-ScheduledTask -TaskName "i茅台自动预约"
```

### 删除任务
```powershell
Unregister-ScheduledTask -TaskName "i茅台自动预约" -Confirm:$false
```

或使用命令提示符：
```cmd
schtasks /delete /tn "i茅台自动预约" /f
```

---

## 常见问题

### Q1: 任务没有运行
**解决**：
1. 检查计算机是否在 09:05 开机
2. 检查任务计划程序服务是否运行
3. 查看任务历史记录（任务计划程序库 → 右侧 → "启用所有任务历史记录"）

### Q2: 运行但没有推送消息
**解决**：
1. 检查 run_reservation.bat 路径是否正确
2. 手动运行 run_reservation.bat 测试
3. 查看日志文件（如果有的话）

### Q3: 提示权限不足
**解决**：
1. 确保以管理员身份运行
2. 在任务属性中勾选 "使用最高权限运行"

### Q4: 修改运行时间
**解决**：
1. 打开任务属性
2. 切换到 "触发器" 标签
3. 编辑触发器，修改开始时间

---

## 文件说明

| 文件 | 说明 |
|------|------|
| `run_reservation.bat` | 实际运行的批处理脚本 |
| `setup_scheduled_task.ps1` | PowerShell 自动安装脚本 |
| `setup_scheduled_task.bat` | 批处理自动安装脚本 |
| `main.py` | 主程序入口 |

---

## 预约时间段

根据 i茅台 官方政策：
- **每日上午 9:00-12:00** 可预约申购
- 建议设置在 **9:05-9:30** 之间运行

推荐运行时间：
- 09:05（推荐，避开高峰）
- 09:10
- 09:15

---

## 下一步

定时任务配置完成后：

1. ✅ 任务会在每天 09:05 自动运行
2. ✅ 预约成功会推送到微信（Server酱）
3. ✅ 可以在任务计划程序中查看运行历史

---

## 需要帮助？

如果遇到问题：
1. 查看任务历史记录
2. 手动运行 `run_reservation.bat` 测试
3. 检查系统时间是否正确

祝预约成功！🍶
